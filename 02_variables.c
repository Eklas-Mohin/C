#include <stdio.h> // Preprocessor directive

int main() {
    int a; // Variable declaration
    a = 6; // Variable initialization
    printf("Value stored in a is %d\n", a);
    a = 1588; // Value can be changed
    printf("Value stored in a is %d\n", a);
    return 0;
}